# Day 002: Những câu lệnh "Sinh tồn" trong Terminal

## 🎯 Mục tiêu
Học cách di chuyển và quản lý file trong máy tính bằng dòng lệnh thay vì dùng chuột.

## 🛠 5 Lệnh cơ bản phải nhớ
Hãy mở Terminal (hoặc Node.js command prompt như hôm qua) và thực hành ngay:

1.  **`pwd`** (Print Working Directory): Bạn đang đứng ở thư mục nào?
2.  **`ls`** (List): Trong thư mục này có những file/thư mục gì? (Trên Windows dùng lệnh `dir`).
3.  **`cd <tên thư mục>`** (Change Directory): Đi vào một thư mục. 
    - Mẹo: `cd ..` để quay lại thư mục cha.
4.  **`mkdir <tên mới>`** (Make Directory): Tạo một thư mục mới.
5.  **`touch <tên file>`** (Tạo file mới): Tạo một file rỗng (Trên Windows có thể dùng: `type nul > filename.txt`).

## ⚡ Thử thách 15 phút (Thực hành thực tế)
Hãy thực hiện chuỗi hành động sau chỉ bằng Terminal:
1. Gõ `cd Desktop` để ra màn hình chính.
2. Gõ `mkdir Nexus_Test` để tạo thư mục tên Nexus_Test.
3. Gõ `cd Nexus_Test` để đi vào thư mục đó.
4. Tạo một file tên là `hello.txt`. Sử dụng lệnh **`type nul > hello.txt`** trên Node.js hoặc **`ni hello.txt`** trên PowerShell hoặc **`touch hello.txt`** trên Linux / macOS / Git Bash.
5. Gõ `ls` (hoặc `dir`) để kiểm tra xem file đã xuất hiện chưa.

## ✅ Hoàn thành
Nếu bạn tạo được file mà không cần click chuột phải, bạn đã xong Ngày 2! Hãy cập nhật cảm nhận của bạn vào đây.
